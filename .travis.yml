language: node_js
node_js:
  - "7"
  - "6"
dist: trusty
sudo: required
addons:
  srcclr: true
  sonarqube:
    organization: "$SONAR_ORGANIZATION"
    token:
      secure: "$SONAR_TOKEN"
    branches:
      - master
      - develop
jdk:
  - oraclejdk8
script:
  - npm test
  - sonar-scanner
  - cat coverage/lcov.info | node_modules/.bin/coveralls
cache:
  directories:
    - '$HOME/.sonar/cache'
