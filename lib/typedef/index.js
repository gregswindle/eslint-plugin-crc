/**
 * @fileOverview
 * A module with custom
 * {@link http://usejsdoc.org/tags-typedef.html @typedef}
 * (type defintions) for JSDoc parsers.
 *
 * @module crc/typedef
 * @memberOf module:crc
 */

const JsonString = require("./json-string");

module.exports = {
  JsonString
};
