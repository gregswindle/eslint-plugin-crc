machine:
  node:
    version: 6
    version: 7
test:
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/reports/coverage
    - mv coverage $CIRCLE_ARTIFACTS/reports
    - npm run docs2html
    - mv docs $CIRCLE_ARTIFACTS/reports
    - codecov
